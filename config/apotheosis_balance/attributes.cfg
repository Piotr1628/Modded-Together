# File Specification: https://gist.github.com/Shadows-of-Fire/88ac714a758636c57a52e32ace5474c1

# Apotheosis Balancer - Attribute Multipliers

"minecraft:generic" {

    armor {
        # Multiplier for generic.armor attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    armor_toughness {
        # Multiplier for generic.armor_toughness attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    attack_damage {
        # Multiplier for generic.attack_damage attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    attack_knockback {
        # Multiplier for generic.attack_knockback attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    attack_speed {
        # Multiplier for generic.attack_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    burning_time {
        # Multiplier for generic.burning_time attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    explosion_knockback_resistance {
        # Multiplier for generic.explosion_knockback_resistance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    fall_damage_multiplier {
        # Multiplier for generic.fall_damage_multiplier attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    flying_speed {
        # Multiplier for generic.flying_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    follow_range {
        # Multiplier for generic.follow_range attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    gravity {
        # Multiplier for generic.gravity attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    jump_strength {
        # Multiplier for generic.jump_strength attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    knockback_resistance {
        # Multiplier for generic.knockback_resistance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    luck {
        # Multiplier for generic.luck attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    max_absorption {
        # Multiplier for generic.max_absorption attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    max_health {
        # Multiplier for generic.max_health attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    movement_efficiency {
        # Multiplier for generic.movement_efficiency attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    movement_speed {
        # Multiplier for generic.movement_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    oxygen_bonus {
        # Multiplier for generic.oxygen_bonus attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    safe_fall_distance {
        # Multiplier for generic.safe_fall_distance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    scale {
        # Multiplier for generic.scale attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    step_height {
        # Multiplier for generic.step_height attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    water_movement_efficiency {
        # Multiplier for generic.water_movement_efficiency attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"minecraft:player" {

    block_break_speed {
        # Multiplier for player.block_break_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    block_interaction_range {
        # Multiplier for player.block_interaction_range attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    entity_interaction_range {
        # Multiplier for player.entity_interaction_range attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    mining_efficiency {
        # Multiplier for player.mining_efficiency attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    sneaking_speed {
        # Multiplier for player.sneaking_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    submerged_mining_speed {
        # Multiplier for player.submerged_mining_speed attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    sweeping_damage_ratio {
        # Multiplier for player.sweeping_damage_ratio attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"minecraft:zombie" {

    spawn_reinforcements {
        # Multiplier for zombie.spawn_reinforcements attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"combat_roll:distance" {
    # Multiplier for distance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"combat_roll:recharge" {
    # Multiplier for recharge attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"combat_roll:count" {
    # Multiplier for count attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"neoforge:swim_speed" {
    # Multiplier for swim_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"neoforge:nametag_distance" {
    # Multiplier for nametag_distance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"neoforge:creative_flight" {
    # Multiplier for creative_flight attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:max_stamina" {
    # Multiplier for max_stamina attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:stamina_efficiency" {
    # Multiplier for stamina_efficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:stamina_recovery" {
    # Multiplier for stamina_recovery attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:paragliding_stamina_efficiency" {
    # Multiplier for paragliding_stamina_efficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:running_stamina_efficiency" {
    # Multiplier for running_stamina_efficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"paraglider:swimming_stamina_efficiency" {
    # Multiplier for swimming_stamina_efficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"caelus:fall_flying" {
    # Multiplier for fall_flying attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"familiarslib:familiar_damage" {
    # Multiplier for familiar_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"familiarslib:familiar_resist" {
    # Multiplier for familiar_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm:additional_critical_damage" {
    # Multiplier for additional_critical_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm:nature_heal" {
    # Multiplier for nature_heal attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm:eat_speed" {
    # Multiplier for eat_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm:charge_time" {
    # Multiplier for charge_time attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:max_mana" {
    # Multiplier for max_mana attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:mana_regen" {
    # Multiplier for mana_regen attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:cooldown_reduction" {
    # Multiplier for cooldown_reduction attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:spell_power" {
    # Multiplier for spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:spell_resist" {
    # Multiplier for spell_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:cast_time_reduction" {
    # Multiplier for cast_time_reduction attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:summon_damage" {
    # Multiplier for summon_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:casting_movespeed" {
    # Multiplier for casting_movespeed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:fire_magic_resist" {
    # Multiplier for fire_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:ice_magic_resist" {
    # Multiplier for ice_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:lightning_magic_resist" {
    # Multiplier for lightning_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:holy_magic_resist" {
    # Multiplier for holy_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:ender_magic_resist" {
    # Multiplier for ender_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:blood_magic_resist" {
    # Multiplier for blood_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:evocation_magic_resist" {
    # Multiplier for evocation_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:nature_magic_resist" {
    # Multiplier for nature_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:eldritch_magic_resist" {
    # Multiplier for eldritch_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:fire_spell_power" {
    # Multiplier for fire_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:ice_spell_power" {
    # Multiplier for ice_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:lightning_spell_power" {
    # Multiplier for lightning_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:holy_spell_power" {
    # Multiplier for holy_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:ender_spell_power" {
    # Multiplier for ender_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:blood_spell_power" {
    # Multiplier for blood_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:evocation_spell_power" {
    # Multiplier for evocation_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:nature_spell_power" {
    # Multiplier for nature_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"irons_spellbooks:eldritch_spell_power" {
    # Multiplier for eldritch_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"lodestone:magic_resistance" {
    # Multiplier for magic_resistance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"lodestone:magic_proficiency" {
    # Multiplier for magic_proficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"lodestone:magic_damage" {
    # Multiplier for magic_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"eternal_starlight:generic" {

    thrown_potion_distance {
        # Multiplier for generic.thrown_potion_distance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    ether_resistance {
        # Multiplier for generic.ether_resistance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    fire_resistance {
        # Multiplier for generic.fire_resistance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    meteor_counterattack_chance {
        # Multiplier for generic.meteor_counterattack_chance attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    heal_multiplier {
        # Multiplier for generic.heal_multiplier attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    enemy_follow_range_multiplier {
        # Multiplier for generic.enemy_follow_range_multiplier attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"eternal_starlight:player" {

    fog_vision {
        # Multiplier for player.fog_vision attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"aces_spell_utils:mana_steal" {
    # Multiplier for mana_steal attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:mana_rend" {
    # Multiplier for mana_rend attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:goliath_slayer" {
    # Multiplier for goliath_slayer attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:hunger_steal" {
    # Multiplier for hunger_steal attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:spell_res_penetration" {
    # Multiplier for spell_res_penetration attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:evasive" {
    # Multiplier for evasive attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:magic_damage_crit_chance" {
    # Multiplier for magic_damage_crit_chance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:magic_damage_crit_damage" {
    # Multiplier for magic_damage_crit_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:magic_projectile_crit_chance" {
    # Multiplier for magic_projectile_crit_chance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:magic_projectile_crit_damage" {
    # Multiplier for magic_projectile_crit_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:magic_projectile_damage" {
    # Multiplier for magic_projectile_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:life_recovery" {
    # Multiplier for life_recovery attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:ritual_magic_resist" {
    # Multiplier for ritual_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:ritual_spell_power" {
    # Multiplier for ritual_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:hydro_magic_resist" {
    # Multiplier for hydro_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:hydro_spell_power" {
    # Multiplier for hydro_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:technomancy_magic_resist" {
    # Multiplier for technomancy_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"aces_spell_utils:technomancy_spell_power" {
    # Multiplier for technomancy_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:armor_pierce" {
    # Multiplier for armor_pierce attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:armor_shred" {
    # Multiplier for armor_shred attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:arrow_damage" {
    # Multiplier for arrow_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:arrow_velocity" {
    # Multiplier for arrow_velocity attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:cold_damage" {
    # Multiplier for cold_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:crit_chance" {
    # Multiplier for crit_chance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:crit_damage" {
    # Multiplier for crit_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:current_hp_damage" {
    # Multiplier for current_hp_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:dodge_chance" {
    # Multiplier for dodge_chance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:draw_speed" {
    # Multiplier for draw_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:experience_gained" {
    # Multiplier for experience_gained attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:fire_damage" {
    # Multiplier for fire_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:ghost_health" {
    # Multiplier for ghost_health attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:healing_received" {
    # Multiplier for healing_received attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:life_steal" {
    # Multiplier for life_steal attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:mining_speed" {
    # Multiplier for mining_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:overheal" {
    # Multiplier for overheal attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:projectile_damage" {
    # Multiplier for projectile_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:prot_pierce" {
    # Multiplier for prot_pierce attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:prot_shred" {
    # Multiplier for prot_shred attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes:elytra_flight" {
    # Multiplier for elytra_flight attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:radiance_magic_resist" {
    # Multiplier for radiance_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:shadow_magic_resist" {
    # Multiplier for shadow_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:cosmic_magic_resist" {
    # Multiplier for cosmic_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:radiance_spell_power" {
    # Multiplier for radiance_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:shadow_spell_power" {
    # Multiplier for shadow_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"hazentouvelib:cosmic_spell_power" {
    # Multiplier for cosmic_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"ars_nouveau:ars_nouveau" {

    perk {

        warding {
            # Multiplier for ars_nouveau.perk.warding attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        mana_regen {
            # Multiplier for ars_nouveau.perk.mana_regen attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        max_mana {
            # Multiplier for ars_nouveau.perk.max_mana attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        spell_damage {
            # Multiplier for ars_nouveau.perk.spell_damage attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        saturation {
            # Multiplier for ars_nouveau.perk.saturation attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        wixie {
            # Multiplier for ars_nouveau.perk.wixie attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        drygmy {
            # Multiplier for ars_nouveau.perk.drygmy attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        feather {
            # Multiplier for ars_nouveau.perk.feather attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        weight {
            # Multiplier for ars_nouveau.perk.weight attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

    }

}


"ars_nouveau:sauce" {

    perk {

        summon_power {
            # Multiplier for sauce.perk.summon_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        summon_resistance {
            # Multiplier for sauce.perk.summon_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        abjuration_power {
            # Multiplier for sauce.perk.abjuration_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        abjuration_resistance {
            # Multiplier for sauce.perk.abjuration_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        necromancy_power {
            # Multiplier for sauce.perk.necromancy_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        necromancy_resistance {
            # Multiplier for sauce.perk.necromancy_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        manipulation_power {
            # Multiplier for sauce.perk.manipulation_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        manipulation_resistance {
            # Multiplier for sauce.perk.manipulation_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        air_power {
            # Multiplier for sauce.perk.air_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        air_resistance {
            # Multiplier for sauce.perk.air_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        earth_power {
            # Multiplier for sauce.perk.earth_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        earth_resistance {
            # Multiplier for sauce.perk.earth_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        fire_power {
            # Multiplier for sauce.perk.fire_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        fire_resistance {
            # Multiplier for sauce.perk.fire_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        water_power {
            # Multiplier for sauce.perk.water_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        water_resistance {
            # Multiplier for sauce.perk.water_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        elemental_power {
            # Multiplier for sauce.perk.elemental_power attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        elemental_resistance {
            # Multiplier for sauce.perk.elemental_resistance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        mana_discount {
            # Multiplier for sauce.perk.mana_discount attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        spell_crit_damage_modifier {
            # Multiplier for sauce.perk.spell_crit_damage_modifier attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

        spell_crit_chance {
            # Multiplier for sauce.perk.spell_crit_chance attribute.
            # Applied ON TOP of the global Apotheosis multiplier.
            # Final value = global_multiplier * attribute_multiplier * base_value
            # Examples:
            #   1.0 = no change (uses global multiplier only)
            #   0.5 = reduce this attribute by 50%
            #   2.0 = double this attribute
            # Default: 1.0; Range: [0.0 ~ 100.0]
            S:multiplier=1.0
        }

    }

}


"eidolon_repraised:chanting_speed" {
    # Multiplier for chanting_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"eidolon_repraised:persistent_soul_hearts" {
    # Multiplier for persistent_soul_hearts attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"eidolon_repraised:max_soul_hearts" {
    # Multiplier for max_soul_hearts attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"eidolon_repraised:magic_power" {
    # Multiplier for magic_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:scythe_proficiency" {
    # Multiplier for scythe_proficiency attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:spirit_spoils" {
    # Multiplier for spirit_spoils attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:arcane_resonance" {
    # Multiplier for arcane_resonance attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:healing_received" {
    # Multiplier for healing_received attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:soul_ward_integrity" {
    # Multiplier for soul_ward_integrity attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:soul_ward_recovery_rate" {
    # Multiplier for soul_ward_recovery_rate attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:soul_ward_recovery_gain" {
    # Multiplier for soul_ward_recovery_gain attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:soul_ward_capacity" {
    # Multiplier for soul_ward_capacity attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:charge_duration" {
    # Multiplier for charge_duration attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:charge_capacity" {
    # Multiplier for charge_capacity attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:charge_recovery_rate" {
    # Multiplier for charge_recovery_rate attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:geas_limit" {
    # Multiplier for geas_limit attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:malignant_conversion" {
    # Multiplier for malignant_conversion attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:malignant_aegis_recovery_rate" {
    # Multiplier for malignant_aegis_recovery_rate attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:malignant_aegis_recovery_gain" {
    # Multiplier for malignant_aegis_recovery_gain attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"malum:malignant_aegis_capacity" {
    # Multiplier for malignant_aegis_capacity attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes_extension:mining_fortune" {
    # Multiplier for mining_fortune attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes_extension:mob_looting" {
    # Multiplier for mob_looting attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes_extension:fishing_speed" {
    # Multiplier for fishing_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"apothic_attributes_extension:fishing_luck" {
    # Multiplier for fishing_luck attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"scguns:projectile_speed" {
    # Multiplier for projectile_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"scguns:additional_bullet_damage" {
    # Multiplier for additional_bullet_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"scguns:reload_speed" {
    # Multiplier for reload_speed attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"scguns:bullet_damage_multiplier" {
    # Multiplier for bullet_damage_multiplier attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"scguns:spread_multiplier" {
    # Multiplier for spread_multiplier attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"sable:player" {

    sub_level_punch_strength {
        # Multiplier for player.sub_level_punch_strength attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

    sub_level_punch_cooldown {
        # Multiplier for player.sub_level_punch_cooldown attribute.
        # Applied ON TOP of the global Apotheosis multiplier.
        # Final value = global_multiplier * attribute_multiplier * base_value
        # Examples:
        #   1.0 = no change (uses global multiplier only)
        #   0.5 = reduce this attribute by 50%
        #   2.0 = double this attribute
        # Default: 1.0; Range: [0.0 ~ 100.0]
        S:multiplier=1.0
    }

}


"primal:reflected_damage" {
    # Multiplier for reflected_damage attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm_spellbooks:abyssal_magic_resist" {
    # Multiplier for abyssal_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm_spellbooks:abyssal_spell_power" {
    # Multiplier for abyssal_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm_spellbooks:technomancy_magic_resist" {
    # Multiplier for technomancy_magic_resist attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


"cataclysm_spellbooks:technomancy_spell_power" {
    # Multiplier for technomancy_spell_power attribute.
    # Applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:multiplier=1.0
}


